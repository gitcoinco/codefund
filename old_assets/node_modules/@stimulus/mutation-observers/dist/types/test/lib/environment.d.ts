import { Recorder } from "./delegate_recorders";
export interface Observer {
    start(): Function;
    stop(): Function;
}
export declare class TestEnvironment {
    element: Element;
    childElement: Element;
    attributeName: string;
    recorder: Recorder;
    observer: Observer;
    static setup(observerConstructor: any, recorderConstructor: any): TestEnvironment;
    constructor(observerConstructor: any, recorderConstructor: any);
    setup(): void;
    teardown(): void;
}
